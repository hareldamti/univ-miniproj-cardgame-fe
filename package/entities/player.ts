export type GameState = {
    
}

export type PlayerState = {
    Cards: GameCard[],
    Settlements: BoardNode[],
    Cities: BoardNode[],
    Roads: BoardEdge[],
}

export type GameCard = {

}

export type BoardNode = {

};

export type BoardEdge = {

};
